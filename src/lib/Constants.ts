export enum Constants {
  PRIMARY_TIMEOUT = 4500,
  SECONDARY_TIMEOUT = 5000
}

export enum Priority {
  PRIMARY = 'primary',
  SECONDARY = 'secondary',
  EXCLUDE = 'exclude'
}

export default Constants
